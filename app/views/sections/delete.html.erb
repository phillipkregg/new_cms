

<%= link_to "<< Back to list", :action => 'list', :page_id => @page.id %>


<%= form_for(:section, :url => { :action => 'destroy', :id => @section.id }) do |f|%>

<p>Are you sure you want to delete <%= @section.name %>?</p>

<%= submit_tag "Destroy Section" %>

<% end %>

