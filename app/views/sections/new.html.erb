
<%= link_to "Back to List", {:action => 'list', :page_id => @page.id} %>

<h2>Create a New Section</h2>

<%= form_for(:section, :url => {:action => 'create', :page_id => @page.id}) do |f| %>
	
	<%= render(:partial => 'form', :locals => {:f => f}) %>
	
	<div>
		<%= submit_tag "Create Section" %>
	</div>
	
<% end %>